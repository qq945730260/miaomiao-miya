services:
  - type: web
    branch: main
    name: miaomiao-miya
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: GH_TOKEN
        sync: false
